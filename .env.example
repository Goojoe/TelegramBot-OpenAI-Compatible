# Environment variables for the Telegram Bot

# Required: Your Telegram Bot Token obtained from BotFather
TELEGRAM_BOT_TOKEN="YOUR_TELEGRAM_BOT_TOKEN_HERE"

# Optional: API Key for the default OpenAI endpoint defined in bot_config.yaml
# If you use ${OPENAI_API_KEY} in your config, set this variable.
OPENAI_API_KEY="YOUR_OPENAI_API_KEY_HERE"

# Optional: API Key for another provider defined in bot_config.yaml
# If you use ${ANOTHER_PROVIDER_KEY} in your config, set this variable.
ANOTHER_PROVIDER_KEY="YOUR_OTHER_PROVIDER_API_KEY_HERE"

# Optional: Base URL for the webhook.
# For local development with ngrok, it might look like: https://your-ngrok-subdomain.ngrok.io
# For Hugging Face Spaces, this is usually set automatically by the platform.
# If not set, defaults to http://localhost:8000 in main.py (only suitable for local testing setup)
# WEBHOOK_BASE_URL="YOUR_PUBLICLY_ACCESSIBLE_BASE_URL"

# Add other environment variables referenced in your bot_config.yaml here
# e.g., CUSTOM_API_KEY="YOUR_CUSTOM_KEY"
